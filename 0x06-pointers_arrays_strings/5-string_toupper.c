#include "main.h"
/** 
  *string_to upper - covert srting to upper 
  * @: given str
  *
  * Return: char
  */
char *string_toupper(char *)
{
	char *cap;

	for (i = 'a' i <= 'z; i++)
	{
	}
	return (cap);
}
