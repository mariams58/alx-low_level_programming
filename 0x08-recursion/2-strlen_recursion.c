#include "main.h"
/**
  * _strlen_recursion - returns string of length
  * @s: given string
  *
  * Return: string of length
  */
int _strlen_recursion(char *s)
{
	int i;

	if (s[i] == '\0')
	{
		return (i - 1);
	{
	return (_strlen_recursion(s + 1));
}
