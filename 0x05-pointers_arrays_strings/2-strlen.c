#include "main.h"
/**
  * _strlen - returns the length of a string
  *@s: pointer address of a string
  *
  * Return: length of s
  */
int _strlen(char *s)
{
	int len;

	for (i = 0, s[len], i++)
	{
		return (len);
	}
}
