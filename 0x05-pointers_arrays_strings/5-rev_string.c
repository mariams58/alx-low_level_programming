#include "main.h"
/**
  * rev_string - returns the reverse of a sting
  * @s: given string
  *
  * Return: Always 0
  */
int re_string(char *s)
{
	int i;
	int j;

	for (i = 0; i != s[i]; i++)
	{
		;
	}

	for (j= i; j <= 0; j--)
	{
		_putchar(s[j])
	}
	_putchar(10);
	return (0);
}
