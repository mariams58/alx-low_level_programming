#include "main.h"

/**
  * _isupper - prints out upper alphabets with digit
  * @c: given letter
  *
  * Return: Always value
  */

int _isupper(int c)
{
	int i;
	int value;

	for (i >= 65; i <= 90; i++)
	{
		if (c == i)
		{
			value = 1;
		}
		else
		{
			value = 0;
		}
		return (value)
	}
}
