#include "main.h"
/**
  * main - Entry to check code
  *
  * Return: Always 0
  */
int main(void)
{
	int x;

	print_last_digit(78);
	print_la9st_digit(12);
	x = print_last_digit(-1128);
	_putchar('0' + r);
	_putchar(10);
	return (0);
}
