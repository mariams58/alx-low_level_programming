#include <stdio.h>

/**
  * main - prints out the numbers of base ten
  *
  * Return: Always 0
  */

int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar(10);
	return (0);
}
